# Auto-generated by changeguard bootstrap-consumer.sh
# 매주 changeguard upstream main을 폴링하고, 새 SHA가 있으면 갱신 PR 생성.

name: policy-bump-watcher

on:
  schedule:
    - cron: '0 9 * * 1'   # 매주 월요일 09:00 UTC
  workflow_dispatch: {}

permissions:
  contents: write
  pull-requests: write

jobs:
  bump:
    uses: sapadmin-df/changeguard/.github/workflows/policy-bump-watcher.yml@c8f0e2ca1aa390ad4aeed18f42745358e420e380
    with:
      policy_sha: c8f0e2ca1aa390ad4aeed18f42745358e420e380
    secrets:
      SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
