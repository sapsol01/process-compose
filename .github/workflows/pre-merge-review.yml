# Auto-generated by changeguard bootstrap-consumer.sh
# 정책 본부: https://github.com/sapadmin-df/changeguard
# 갱신: policy-bump-watcher.yml 가 매주 갱신 PR을 제안 (수동 merge).

name: pre-merge-review

on:
  push:
    branches: [main]
  pull_request: {}

permissions:
  contents: read
  pull-requests: write

jobs:
  review:
    uses: sapadmin-df/changeguard/.github/workflows/pre-merge-review.yml@c8f0e2ca1aa390ad4aeed18f42745358e420e380
    with:
      policy_sha: c8f0e2ca1aa390ad4aeed18f42745358e420e380
    secrets:
      ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
      SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
